#include<stdio.h>
#include<math.h>

int main()
{
    int n, i;

    scanf("%d", &n);
    int t[n];

    for(i=0;i<n;i++)
    {
        scanf("%d", &t[i]);

        if(i>1)
        {
            if(i>)
        }
        else
        {
            printf("20\n");
        }
    }

    return 0;
}
